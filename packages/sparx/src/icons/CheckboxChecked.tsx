import * as React from "react";

import UniCheckSquare from "@iconscout/react-unicons-solid/icons/uis-check-square";

import { IconProps } from "./IconProps";

export default function CheckboxChecked(props: IconProps) {
  return <UniCheckSquare {...props} />;
}
