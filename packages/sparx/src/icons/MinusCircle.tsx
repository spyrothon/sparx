import * as React from "react";

import UniMinusCircle from "@iconscout/react-unicons/icons/uil-minus-circle";

import { IconProps } from "./IconProps";

export default function MinusCircle(props: IconProps) {
  return <UniMinusCircle {...props} />;
}
