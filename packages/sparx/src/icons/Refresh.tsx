import * as React from "react";

import UniSync from "@iconscout/react-unicons/icons/uil-sync";

import { IconProps } from "./IconProps";

export default function Refresh(props: IconProps) {
  return <UniSync {...props} />;
}
