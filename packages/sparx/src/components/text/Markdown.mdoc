# Markdown

`Markdown` is a component that enables rendering raw Markdown content into components that match the rest of the Design System.

{% showcase %}
```
<Markdown>
  Here's a markdown component, it has **bold** and _italic_ support, [plus more](#).
</Markdown>
```
{% /showcase %}
